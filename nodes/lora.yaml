# Home Assistant Package

template:
#2
  - sensor:
###4
    - name: "LoRa ID Status"
      state: >
        {% set data = states('sensor.gatewaylora_2')|string %}
        {% set id = data.split('|')[1]|string %}
        {% if id == "id" %}
          {{ "ok" }}
        {% endif %}

# EOF
